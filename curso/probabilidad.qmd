# Repaso de probabilidad {.unnumbered}

::: {.content-hidden}
$$
{{< include ../assets/includes/_macros.tex >}}
$$
:::

Lo que este curso da por sabido de probabilidad. No se ve en clase:
se supone conocido de los cursos previos y se recoge aquí para poder consultarlo.

## Variables aleatorias continuas

Una variable aleatoria continua $X$ queda descrita por su **densidad** $\pdf$, que cumple
$\pdf(x) \geq 0$ y $\int_{-\infty}^{\infty} \pdf(x)\,\dd x = 1$. La probabilidad de caer en
un intervalo es el área bajo la densidad:
$$
\Prob{a \leq X \leq b} = \int_a^b \pdf(x)\,\dd x .
$$

Una densidad **no** es una probabilidad: puede valer más que 1. Lo que está acotado por 1
es su integral.

**Esperanza y varianza.**
$$
\E{X} = \int x\,\pdf(x)\,\dd x,
\qquad
\Var{X} = \E{(X - \E{X})^2} = \E{X^2} - \E{X}^2 .
$$

Una propiedad importante.

::: {#lem-esperanza-funcion}
## La esperanza de una función de una extracción

Sea $X$ una variable aleatoria con densidad $\pdf$ y sea $g$ una función **fija**, elegida
sin mirar los datos, con $\int\abs{g(x)}\,\pdf(x)\,\dd x<\infty$. Entonces

$$
\Esub{X}{g(X)}=\int g(x)\,\pdf(x)\,\dd x .
$$ {#eq-esperanza-funcion}

En particular, si $X_1,\ldots,X_{\nobs}$ son extracciones independientes de $\pdf$, entonces
$\E{g(X_i)}$ es esa misma integral para todo $i$: todas las extracciones tienen la misma
esperanza. Vale igual para un vector aleatorio con distribución conjunta $P$, escribiendo
$\Esub{P}{g(X,Y)}$.
:::


**Respecto de qué se toma una esperanza.** En todo el libro, el subíndice de $\E{\cdot}$
nombra lo que se promedia, y se escribe siempre que haya más de un candidato posible:

| Notación | Se promedia sobre | Qué integral es |
|---|---|---|
| $\Esub{P^\star}{h(X,Y)}$ | una observación nueva $(X,Y)$ extraída de la distribución $P^\star$ que genera los datos | $\int h(\xv,y)\,\pdf^\star(\xv,y)\,\dd\xv\,\dd y$ |
| $\Esub{X}{h(X)}$ | la distribución marginal de las variables predictoras | $\int h(\xv)\,\pdf_X(\xv)\,\dd\xv$ |
| $\Esub{\varepsilon}{h(\varepsilon)}$ | el ruido de una observación | $\int h(z)\,\pdf_\varepsilon(z)\,\dd z$ |
| $\Esub{\Dtrain}{h(\Dtrain)}$ | las muestras de entrenamiento posibles, cada una $\nobs$ extracciones independientes de $P^\star$ | $\int h(\data)\,\prod_{i=1}^{\nobs}\pdf^\star(\xv_i,y_i)\,\dd\data$ |

Las de $\Esub{\data}{\cdot}$ y $\Esub{\Dtest}{\cdot}$ son la última con el tamaño y el papel
que indique el subíndice. En todas, $h$ es la cantidad que va dentro de la esperanza, y la
integral se sustituye por una suma si la variable es discreta.

Cuando dentro de la esperanza hay una sola variable aleatoria y no cabe ambigüedad, como en
$\E{\varepsilon}$ o $\E{X}$, el subíndice se omite.

Las dos reglas que usaremos con más frecuencia:

::: {#lem-linealidad}
## Linealidad y escala

Para $a, b$ constantes, $\E{aX + b} = a\E{X} + b$ y $\Var{aX+b} = a^2\Var{X}$.
:::


Además, $\E{X + Y} = \E{X} + \E{Y}$ sin ninguna hipótesis, mientras que
$\Var{X+Y} = \Var{X} + \Var{Y}$ **solo si $X$ e $Y$ son independientes**. En general,
$\Var{X+Y} = \Var{X} + \Var{Y} + 2\Cov{X}{Y}$.

## Independencia

Las variables $X_1,\dots,X_{\nobs}$ son **independientes** si su densidad conjunta es el
producto de las densidades individuales:
$$
\pdf(x_1,\dots,x_{\nobs}) = \prod_{i=1}^{\nobs} \pdf_i(x_i).
$$

De la factorización se siguen las tres consecuencias que se usan sin volver a justificarlas.
La primera es que la esperanza de un producto se parte:
$\E{XY} = \E{X}\E{Y}$, y por tanto $\Cov{X}{Y} = 0$. La segunda es que la varianza de una
suma se parte también, $\Var{X_1+\dots+X_\nobs} = \Var{X_1}+\dots+\Var{X_\nobs}$, lo que
para $\nobs$ extracciones de la misma distribución con varianza $\noise^2$ da
$\Var{\bar X} = \noise^2/\nobs$: promediar reduce la varianza en un factor $\nobs$. La
tercera es que la densidad conjunta de una muestra es el producto de las individuales, que
es lo que permite escribir la verosimilitud de un conjunto de datos.

Independencia es más fuerte que covarianza cero. Que $\Cov{X}{Y}=0$ solo dice que no hay
relación lineal; puede haberla de otro tipo, y entonces las variables no son independientes.

## Cambio de variable

Si $Y = g(X)$ con $g$ derivable y estrictamente monótona, la densidad de $Y$ es
$$
\pdf_Y(y) = \pdf_X\!\left(g^{-1}(y)\right) \left| \frac{\dd}{\dd y} g^{-1}(y) \right| .
$$

El factor de la derecha es el **jacobiano**, y es lo que impide tratar una densidad como
si fuera una probabilidad.

## Esperanza condicional

$\E{Y \given X = x}$ es la media de $Y$ entre las observaciones con ese valor de $X$.

::: {#lem-esperanza-total}
## Ley de la esperanza total

$\E{Y} = \E{\,\E{Y \given X}\,}$.
:::

La esperanza de dentro se toma sobre $Y$ con $X$ fijada, y la de fuera sobre $X$. El
resultado dice que promediar por grupos y después promediar los promedios devuelve la media
global, siempre que cada grupo pese lo que le toca. Sirve para calcular una esperanza en dos
pasos cuando el condicionamiento simplifica el primero.

Junto a ella se usa la descomposición análoga de la varianza,
$\Var{Y} = \E{\Var{Y \given X}} + \Var{\E{Y \given X}}$: la variabilidad total se reparte
entre la que queda dentro de cada grupo y la que hay entre los grupos.

## Cuantiles

::: {#def-cuantil}
## Cuantil y mediana

El **cuantil** de orden $\tau \in (0,1)$ de $X$ es el valor $q_\tau$ tal que
$\Prob{X \leq q_\tau} = \tau$. La **mediana** es $q_{0{,}5}$.
:::

::: {#lem-cuantil-creciente}
## Los cuantiles se conservan bajo transformaciones crecientes

Si $g$ es estrictamente creciente, entonces $q_\tau\bigl(g(X)\bigr) =
g\bigl(q_\tau(X)\bigr)$ para todo $\tau \in (0,1)$.
:::

::: {.proof}
Como $g$ es estrictamente creciente, los sucesos $\{g(X) \leq g(q)\}$ y $\{X \leq q\}$ son
el mismo, luego tienen la misma probabilidad. Tomando $q = q_\tau(X)$,

$$
\Prob{g(X) \leq g\bigl(q_\tau(X)\bigr)} = \Prob{X \leq q_\tau(X)} = \tau ,
$$

y por definición eso significa que $g(q_\tau(X))$ es el cuantil de orden $\tau$ de $g(X)$.
:::

La media **no** tiene esa propiedad. Si $g$ no es lineal, en general
$\E{g(X)} \neq g(\E{X})$. Basta un ejemplo con dos valores: si $X$ vale $1$ o $3$ con la
misma probabilidad, $\E{X^2} = 5$ mientras que $\E{X}^2 = 4$. La desigualdad de Jensen dice
además hacia qué lado falla: si $g$ es convexa, $\E{g(X)} \geq g(\E{X})$.

De ahí que la mediana de un precio en euros y la mediana de su logaritmo se correspondan,
y las medias no.

## La distribución normal

::: {#def-normal}
## Distribución normal

Una variable $\varepsilon$ sigue una **distribución normal** de media $\mu$ y varianza
$\noise^2$, y se escribe $\varepsilon \sim \Normal(\mu, \noise^2)$, si su densidad es

$$
\pdf(z;\mu,\noise) = \frac{1}{\noise\sqrt{2\pi}}\,
\exp\!\left(-\frac{(z-\mu)^2}{2\noise^2}\right),
\qquad z \in \Real .
$$ {#eq-normal}

El parámetro $\mu$ sitúa el centro de la campana y $\noise$ dice cuánto se ensancha. La
**normal estándar** es el caso $\mu = 0$, $\noise = 1$.
:::

Algunas propiedades de la normal.

::: {#lem-normalizacion}
## Constante de normalización

$\displaystyle \int_{-\infty}^{\infty} e^{-z^2/2}\, \dd z = \sqrt{2\pi}$.
:::

::: {.cajanegra}
**No lo demostramos**
:::

::: {#thm-traslacion}
## Traslación

Si $\varepsilon \sim \Normal(\mu, \noise^2)$ y $c \in \Real$, entonces
$\varepsilon + c \sim \Normal(\mu + c, \noise^2)$.
:::

::: {.proof}
Sea $W = \varepsilon + c$. Entonces $\Prob{W \leq t} = \Prob{\varepsilon \leq t - c} =
\int_{-\infty}^{t-c} \pdf(x;\mu,\noise)\,\dd x$. El cambio de variable $u = x + c$ (con $\dd
u = \dd x$) transforma la integral en $\int_{-\infty}^{t} \pdf(u - c;\mu,\noise)\,\dd u$, y
sustituyendo en @eq-normal se comprueba que
$\pdf(u-c;\mu,\noise) = \pdf(u; \mu+c, \noise)$, porque restar $c$ a $u$ y sumárselo a
$\mu$ dejan igual la diferencia $u - c - \mu$ que aparece en el exponente.
:::

::: {#thm-escala}
## Escala

Si $\varepsilon \sim \Normal(\mu, \noise^2)$ y $a \neq 0$, entonces
$a\varepsilon \sim \Normal(a\mu, a^2\noise^2)$.
:::

::: {.proof}
Tomemos $a > 0$; el caso $a<0$ es idéntico salvo que se invierten los límites y aparece
$\abs{a}$. Sea $W = a\varepsilon$. Entonces
$$
\Prob{W \leq t} = \Prob{\varepsilon \leq t/a} = \int_{-\infty}^{t/a} \pdf(x;\mu,\noise)\,\dd
x .
$$
El cambio de variable $u = ax$, con $\dd u = a\,\dd x$, lleva la integral a
$\int_{-\infty}^{t} \frac{1}{a}\,\pdf(u/a;\mu,\noise)\,\dd u$. Basta comprobar que el
integrando es la densidad buscada:
$$
\frac{1}{a}\,\pdf\!\left(\frac{u}{a};\mu,\noise\right)
= \frac{1}{a\noise\sqrt{2\pi}} \exp\!\left(-\frac{(u/a-\mu)^2}{2\noise^2}\right)
= \frac{1}{(a\noise)\sqrt{2\pi}} \exp\!\left(-\frac{(u-a\mu)^2}{2(a\noise)^2}\right),
$$
donde el último paso sale de multiplicar numerador y denominador del exponente por $a^2$.
El resultado es $\pdf(u; a\mu, a\noise)$.
:::

::: {#cor-estandarizacion}
## Estandarización

Si $\varepsilon \sim \Normal(\mu, \noise^2)$ entonces $Z = (\varepsilon - \mu)/\noise \sim
\Normal(0,1)$.
:::

::: {.proof}
Aplicar @thm-escala con $a = 1/\noise$ y después @thm-traslacion con $c = -\mu/\noise$.
:::

::: {#thm-momentos}
## Media y varianza

Si $\varepsilon \sim \Normal(\mu, \noise^2)$ entonces $\E{\varepsilon} = \mu$ y
$\Var{\varepsilon} = \noise^2$.
:::

::: {.proof}
Basta hacerlo para la normal estándar $Z$, porque por @cor-estandarizacion podemos
escribir $\varepsilon = \noise Z + \mu$, y entonces @lem-linealidad da
$\E{\varepsilon} = \noise\E{Z} + \mu$ y $\Var{\varepsilon} = \noise^2\Var{Z}$.

Sea $\varphi(z) = e^{-z^2/2}/\sqrt{2\pi}$ la densidad estándar.

*Media.* El integrando $z\,\varphi(z)$ es una función impar, y su integral sobre toda la
recta es cero. Luego $\E{Z} = 0$.

*Varianza.* Como $\E{Z}=0$, se tiene $\Var{Z} = \E{Z^2} = \int z^2 \varphi(z)\,\dd z$.
Integramos por partes con $u = z$ y $\dd v = z\varphi(z)\,\dd z$. Nótese que
$\varphi'(z) = -z\varphi(z)$, de modo que $v = -\varphi(z)$. Entonces
$$
\int_{-\infty}^{\infty} z^2 \varphi(z)\,\dd z
= \Big[-z\varphi(z)\Big]_{-\infty}^{\infty} + \int_{-\infty}^{\infty} \varphi(z)\,\dd z
= 0 + 1 = 1,
$$
donde el corchete se anula porque $\varphi$ decae más deprisa que cualquier polinomio, y
la última integral vale 1 por @lem-normalizacion.

Sustituyendo, $\E{\varepsilon} = \mu$ y $\Var{\varepsilon} = \noise^2$.
:::

## Otras distribuciones del curso

Aparecen cuando cambiamos el modelo de ruido. Se recogen aquí para tenerlas juntas; cada
una se presenta en su capítulo.

| Distribución | Densidad o masa | Soporte | Dónde |
|---|---|---|---|
| Normal | $\frac{1}{\noise\sqrt{2\pi}}e^{-(x-\mu)^2/2\noise^2}$ | $\Real$ | señal y ruido; verosimilitud y pérdida |
| Laplace | $\frac{1}{2b}e^{-\abs{x-\mu}/b}$ | $\Real$ | verosimilitud y pérdida |
| Lognormal | si $\log Y \sim \Normal(\mu,\noise^2)$ | $(0,\infty)$ | preparación de datos |
| Poisson | $e^{-\lambda}\lambda^{k}/k!$ | $\{0,1,2,\dots\}$ | mapa final, no evaluable |
| Gamma | $\propto x^{\alpha-1}e^{-x/\beta}$ | $(0,\infty)$ | mapa final, no evaluable |


## Qué hay que traer de Estadística

Un **estimador** es una función de la muestra que se usa para aproximar una cantidad
desconocida. Como la muestra es aleatoria, el estimador también lo es: tiene distribución,
esperanza y varianza. Se escribe con acento circunflejo, $\hat\theta$ para estimar $\theta$.

Tres propiedades suyas se usan a lo largo del curso.

- **Insesgado.** Acierta en promedio, $\E{\hat\theta} = \theta$. No dice que acierte en una
  muestra concreta, sino que si el muestreo se repitiera muchas veces, la media de las
  estimaciones sería el valor buscado.
- **Varianza del estimador.** $\Var{\hat\theta}$ mide cuánto cambia la estimación de una
  muestra a otra. Un estimador insesgado con varianza grande acierta en promedio y falla en
  cada intento. A su raíz cuadrada se le llama **error típico**.
- **Consistente.** Se acerca al valor buscado a medida que la muestra crece: $\hat\theta$
  tiende a $\theta$ cuando $\nobs \to \infty$. Si no hay sesgo, basta con que la varianza
  tienda a cero.

El ejemplo de referencia es la media muestral $\bar X$ de $\nobs$ extracciones
independientes de media $\mu$ y varianza $\noise^2$. Es insesgada, porque
$\E{\bar X} = \mu$; su varianza es $\noise^2/\nobs$; y es consistente, porque esa varianza
tiende a cero. Las tres cosas salen de la linealidad de la esperanza y de la varianza de
una suma de independientes.

## Diagnóstico

Quince minutos, sin ordenador y sin calculadora. No cuenta para la nota; sirve para saber de
qué punto partimos. En las cinco primeras preguntas, $X$ tiene densidad $\pdf(x) = 2x$ en el
intervalo $[0,1]$ y cero fuera.

1. Comprueba que $\pdf$ es una densidad y calcula $\Prob{X\leq 1/2}$.
2. Calcula $\E{X}$.
3. Calcula $\E{X^2}$ diciendo qué resultado usas, y comprueba que **no** vale $\E{X}^2$.
4. Con los dos apartados anteriores, calcula $\Var{X}$. Después, sin volver a integrar,
   calcula $\E{3X-1}$ y $\Var{3X-1}$.
5. Sean $X_1, X_2, X_3$ tres extracciones independientes de $\pdf$. Escribe su densidad
   conjunta y di por qué la verosimilitud del curso es un producto.
6. Sea $Z\sim\Normal(0,1)$ y $W = 2Z + 5$. Di de qué distribución es $W$, con sus
   parámetros, y qué resultados del anexo lo justifican.
7. Sea $Y = X + \varepsilon$ con $\varepsilon$ independiente de $X$ y $\E{\varepsilon} = 0$.
   Calcula $\E{Y\mid X}$ y deduce $\E{Y}$ diciendo qué resultado usas.

::: {.callout-note collapse="true"}
## Soluciones del diagnóstico

1. Es no negativa en $[0,1]$ e integra a uno: $\int_0^1 2x\,\dd x = \left[x^2\right]_0^1 =
   1$. Y $\Prob{X\leq 1/2} = \int_0^{1/2} 2x\,\dd x = 1/4$. Fíjate en que no es $1/2$: la
   densidad crece, así que la mitad izquierda pesa menos.
2. $\E{X} = \int_0^1 x\cdot 2x\,\dd x = 2/3$.
3. Por @lem-esperanza-funcion, con $g(x) = x^2$: $\E{X^2} = \int_0^1 x^2\cdot 2x\,\dd x =
   1/2$. Y $\E{X}^2 = 4/9$, que es distinto: la esperanza de una función **no** es la
   función de la esperanza. Es el error que más veces aparece en el curso.
4. $\Var{X} = \E{X^2}-\E{X}^2 = 1/2 - 4/9 = 1/18$. Por @lem-linealidad, $\E{3X-1} = 3\cdot
   2/3 - 1 = 1$ y $\Var{3X-1} = 9\Var{X} = 1/2$: la constante no afecta a la varianza y el
   factor entra al cuadrado.
5. $\pdf(x_1,x_2,x_3) = 8\,x_1x_2x_3$ en el cubo unidad, es decir, el producto de las tres
   densidades, que es la definición de independencia de la sección correspondiente. De ahí
   que la verosimilitud de una muestra independiente se escriba como producto y su logaritmo
   como suma.
6. $W\sim\Normal(5, 4)$. La escala sale de @thm-escala y la traslación de @thm-traslacion;
   juntas dicen que una transformación afín de una normal es normal, con media $2\cdot 0+5$
   y varianza $2^2\cdot 1$. Ojo: la varianza es $4$, no $2$.
7. $\E{Y\mid X} = X + \E{\varepsilon} = X$, porque fijado $X$ lo único aleatorio es el
   ruido. Y por @lem-esperanza-total, $\E{Y} = \E{\E{Y\mid X}} = \E{X}$. Esta es la cuenta
   que sostiene el capítulo 1: el modelo describe la media de la respuesta y el ruido no la
   desplaza.
:::
