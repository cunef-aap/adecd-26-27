Figuras del curso.

Las rescatadas de `curso-ml-R` se usan con permiso del autor de los mazos
`02-supervised` y `04-regression` (Alberto Torres Barrán en la cabecera YAML);
`06-trees` es de Víctor Gallego y Roi Naveiro. En la web hay que mantener la
atribución que corresponda.

Queda una restricción que no depende de ese permiso: algunas imágenes son de
terceros y hay que citarlas o sustituirlas.

- Reutilizables sin problema (Wikipedia): `Gradient_descent.svg`,
  `Newton_optimization_vs_grad_descent.svg`, `biasvariance.svg`, `crossval.jpg`,
  `Logistic-curve.svg`, `laplace.svg`.
- Requieren atribución o sustitución: las de *The Elements of Statistical Learning*
  (`eslr_ex.png`, `knn_class.png`, `curse_dim.png`, `models.png`, `pd_spam.png`) y
  la de xkcd (`machine_learning_2x.png`).
- Llevan marca ICMAT y hay que quitarla: `logo.png` y lo que use el ámbar `#FFAA00`.
